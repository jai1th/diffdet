# Pinned to the environment the run6 model was produced and verified in.
# (CUDA is optional — everything here also loads and runs on CPU with map_location="cpu".)
torch==2.9.1
torchvision==0.24.1
scikit-learn==1.8.0
numpy==2.2.6
opencv-python==4.12.0.88
